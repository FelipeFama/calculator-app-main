import styled from "styled-components";

export const Themes = styled.section`
  display: flex;
  flex-direction: row;
  gap: 1.5rem;
  align-items: flex-end;
  p {
    font-size: 0.75rem;
    letter-spacing: 1px;
  }
`;
